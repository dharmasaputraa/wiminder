# Replicate SQLite to object storage — fill in per provider (S3/R2).
dbs:
  - path: /data/wimember.db
    replicas:
      - url: s3://bucket-anda/wiminder/wimember.db
        # endpoint, access-key-id, secret-access-key via env LITESTREAM_*
