# Re-assign branch commits to dharmasaputraa00@gmail.com

## Current state
- All 11 commits on `feature/channels-settings-refactor` (93a31ae..67fb199) are authored as `dharma.saputra <dharma.saputra@taksu.tech>`.
- The branch is already pushed to origin, so history rewrite requires a force-push.
- No repo-local git identity is set for this folder (falls back to the global `dharma.saputra` identity).

## Steps

1. **Set the local git identity for this repo only** (does not touch global config):
   ```
   git config --local user.name "dharmasaputraa"
   git config --local user.email "dharmasaputraa00@gmail.com"
   ```

2. **Verify the working tree is clean** (`git status --short`) — a rebase needs it; report and stop if anything unexpected is dirty.

3. **Rewrite author + committer on the 11 branch commits** (commit messages, dates, and content unchanged):
   ```
   git rebase main --exec 'git commit --amend --no-edit --reset-author'
   ```
   `--reset-author` re-stamps each commit with the new local identity (both author and committer).

4. **Verify**: `git log --format="%h %an <%ae> | %cn <%ce>" main..HEAD` — every line must show `dharmasaputraa <dharmasaputraa00@gmail.com>` for both author and committer; also confirm the tree/diff vs origin is content-identical (only hashes change).

5. **Force-push the rewritten branch** (safe variant, refuses if someone else pushed meanwhile):
   ```
   git push --force-with-lease origin feature/channels-settings-refactor
   ```

## Notes
- All 11 commit hashes will change (git rewrites them top-down); the branch name, PR link, and file contents are unaffected.
- Only this repo folder uses the new identity — global config stays `dharma.saputra`.
- Future commits in this repo are automatically authored as `dharmasaputraa <dharmasaputraa00@gmail.com>`.
